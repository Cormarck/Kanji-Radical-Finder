import Auswahlflaeche from './auswahl';

function Radikalflaeche({array, clickFunktion, klasse, begriff, act}) {
  
    return (
            <div>
            <Auswahlflaeche klasse = {klasse} array = {array} clickFunktion={clickFunktion} begriff = {begriff} act = {act}/>
            </div>
    )
  }

  export default Radikalflaeche